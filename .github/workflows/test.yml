name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  unittest:
    name: Unit tests + coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.8"
      - run: pip install coverage
      - name: Run tests with coverage
        run: ./run_tests.sh

  sublime-package:
    name: Sublime UnitTesting
    runs-on: ubuntu-latest
    # ST4 UnitTesting integration is best-effort — the SublimeText/UnitTesting
    # action can fail due to API availability timing and Package Control install
    # issues in CI. The unittest job above is the authoritative gate.
    continue-on-error: true
    strategy:
      matrix:
        st-version: [4]
    steps:
      - uses: actions/checkout@v4
      - uses: SublimeText/UnitTesting/actions/setup@v1
        with:
          sublime-text-version: ${{ matrix.st-version }}
          package-name: Autonoma
      - uses: SublimeText/UnitTesting/actions/run-tests@v1
        with:
          package-name: Autonoma
